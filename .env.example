# ให้ browser ส่ง request ไปที่ same-origin (Next.js จะ proxy ให้)
NEXT_PUBLIC_API_URL=

# URL ของ backend จริง — ใช้โดย next.config.ts สำหรับ rewrites (ไม่ถูกส่งไปยัง browser)
BACKEND_URL=http://localhost:4000
